// Regenerated automatically by scripts/write-convex-config.js during
// Vercel builds (see vercel.json) - each preview deployment gets its own
// fresh Convex backend, so this file's contents differ per-deploy. The
// value below is only what's checked into git / used for local dev.
(function () {
  window.NSAA_CONVEX_CONFIG = {
    url: "https://aromatic-iguana-779.convex.cloud",
  };
})();
